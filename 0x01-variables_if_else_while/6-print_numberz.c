#include <stdio.h>
/**
 * main - Function that prints all sngle
 * digit numbers of base ten.
 * Description: None
 * Return: Always 0 (Success).
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);
}
