#include <stdio.h>
/**
 * main - Function that prints all sngle
 * digit numbers of base ten.
 * Description: None
 * Return: Always 0 (Success).
 */
int main(void)
{
	int num;

	for (num = 48; a < 58; num++)
	{
		putchar(num);
	}
	putchar('\n');
	return (0);
}
