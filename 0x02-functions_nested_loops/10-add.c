#include "main.h"
/**
 * add - function that adds two integers and returns the resul
 * @a: Character to check
 * @b: character to check
 * Return: a + b (Success)
 */
int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}

