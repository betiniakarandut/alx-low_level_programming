#include "main.h"
/**
 * _islower - Function that changes to lowercase
 * @c: character to check
 * Return: Return 1 if it is true and
 * 0 if not true
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
