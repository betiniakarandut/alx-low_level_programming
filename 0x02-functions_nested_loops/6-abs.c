#include "main.h"

/**
 * _abs - Function that computes the absolete
 * value of an integer
 * @a: character to check for
 * Return: bon
 */
int _abs(int a)
{
	if (a >= 0)
	{
		return (a);
	}
	return (-a);
}

